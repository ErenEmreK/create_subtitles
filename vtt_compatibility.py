from webvtt import WebVTT, Caption
import sys

def stable_result_to_vtt(result, output_file, plus_time=0):
    #TODO
    print("Creating .vtt files is impossible at the moment.")
    sys.exit()
    
def result_to_vtt(result, output_file, plus_time=0):
    #TODO
    print("Creating .vtt files is only possible with stable models at the moment. Use '-s' command to switch to stable models.")
    sys.exit()